langcode: en
status: true
dependencies:
  config:
    - user.role.tester
  module:
    - user
id: user_remove_role_action.tester
label: 'Remove the Tester role from the selected users'
type: user
plugin: user_remove_role_action
configuration:
  rid: tester
