langcode: en
status: true
dependencies:
  config:
    - user.role.tester
  module:
    - user
id: user_add_role_action.tester
label: 'Add the Tester role to the selected users'
type: user
plugin: user_add_role_action
configuration:
  rid: tester
