langcode: en
status: true
dependencies:
  config:
    - user.role.product_owner
  module:
    - user
id: user_add_role_action.product_owner
label: 'Add the Product Owner role to the selected users'
type: user
plugin: user_add_role_action
configuration:
  rid: product_owner
